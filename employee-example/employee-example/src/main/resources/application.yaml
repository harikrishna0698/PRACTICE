spring:
  application:
    name: employee-example

  datasource:
    url: jdbc:postgresql://localhost:5432/TestDB
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8092
